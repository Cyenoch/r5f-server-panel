# CONTEXT

r5-server 的领域词汇表。实现地图、约束与引擎事实见 `README.md`。
规格与增强点清单见 `.scratch/fleet-ops/spec.md` 与 `.scratch/fleet-ops/roadmap.md`。

## 这是什么

管理 **r5f-dedi**（基于 Apex Legends S21 的 R5Flowstate 专用服务端改版）的 Windows 工具：版本切换、启动/停止/重启、升级与备份、托管控制台日志、在线玩家与审核、模式与公告。单机部署（腾讯云轻量 2C8G/80G，Windows Server 2022），不引入公网管理端口。

## 领域词汇

| 术语             | 含义                                                                                                     |
| ---------------- | -------------------------------------------------------------------------------------------------------- |
| **版本**         | 安装好的服务端发行资源，可被多个实例复用；升级安装新版本，不自动切换实例。                               |
| **实例**         | 持久存在的服务器身份，拥有名称、版本、端口、启动设置和玩法模板；停止后仍保留，同一实例最多运行一个进程。 |
| **运行记录**     | 实例某次启动的进程、控制通道、日志与实际应用配置；不等于实例本身。                                       |
| **工作副本**     | 实例独有的可写游戏文件，隔离配置、公告、封禁和引擎日志；共享版本保持只读。                               |
| **玩法模板**     | 可复用的模式、地图和已验证参数组合；多个实例可绑定同一模板，保存不自动改变正在进行的对局。               |
| **期望配置**     | 已保存、准备在下次启动或明确应用时采用的设置；不代表引擎已经采用。                                       |
| **已应用配置**   | 启动时实际写入或运行期得到引擎确认的配置。静默命令不能据此标记成功。                                     |
| **运行参数应用** | 不主动换图的参数下发；每轮读取和关卡初始化读取的参数有不同生效时机。                                     |
| **重新加载玩法** | 明确切换模式并重新加载地图，会中断当前对局，但不重启服务端进程。                                         |
| **控制通道**     | 仅供本机面板控制引擎的认证通道，不对公网开放。                                                           |
| **回执**         | 引擎对命令的回答：明确成功、未知命令、用法错误或静默；静默不等于成功。                                   |
| **托管控制台**   | 由日志守护转发引擎输出与输入；面板退出不终止服务端。                                                     |
| **模式**         | 游戏发行版提供的玩法条目；地图及候选参数取自发行版，而不是面板虚构。                                     |
| **公告表**       | 实例的轮播与进场文案；保存和游戏重新读取是不同事件。                                                     |
| **机器人**       | 模拟玩家，可添加或踢出，不可封禁。                                                                       |
| **健康**         | 实例本次运行的错误、警告和环境诊断。                                                                     |
| **观测数据**     | 面板实际采集的进程及游戏指标；零、未知、过期和历史值必须区分，面板关闭期间不补造数据。                   |

## 权威来源

- 设计基线：`.scratch/fleet-ops/spec.md`
- 增强点与状态：`.scratch/fleet-ops/roadmap.md`
- 执行契约：`.scratch/fleet-ops/issues/NN-*.md`
- 用户手册：`README.md`
- 桌面端踩坑记录（solid-gpui）：`docs/solid-gpui-notes.md`
